{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "env": {
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  },
  "installCommand": "cd ../.. && npm install -g corepack@latest && corepack enable && corepack prepare pnpm@9.12.0 --activate && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm build"
}
